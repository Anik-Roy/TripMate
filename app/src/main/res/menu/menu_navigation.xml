<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".ExistingEvent">

    <item
        android:id="@+id/homeprofile"
        android:checked="true"
        android:icon="@drawable/homeprofile"
        android:title="My Profile"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/chatMenu"
        android:checked="true"
        android:icon="@drawable/chat"
        android:title="Chat"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/myFriends"
        android:checked="true"
        android:icon="@drawable/friend"
        android:title="My friends"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/users"
        android:checked="true"
        android:icon="@drawable/person"
        android:title="All Travelers"
        app:showAsAction="ifRoom"/>


    <item
        android:id="@+id/interestedEvent"
        android:checked="true"
        android:icon="@drawable/event"
        android:title="Interested Events"
        app:showAsAction="ifRoom"/>
    <item
        android:id="@+id/settings"
        android:checked="true"
        android:icon="@drawable/settings"
        android:title="Settings"
        app:showAsAction="ifRoom"/>

</menu>